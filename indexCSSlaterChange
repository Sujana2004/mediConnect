@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Noto+Sans+Devanagari:wght@400;500;600;700;800&family=Noto+Sans+Telugu:wght@400;500;600;700;800&display=swap');

@tailwind base;
@tailwind components;
@tailwind utilities;

/* ============================================================================
   BASE STYLES
   ============================================================================ */

@layer base {
  html {
    @apply scroll-smooth;
  }
  
  body {
    @apply bg-gray-50 text-gray-900 antialiased;
    font-family: var(--font-family-primary, 'Inter', sans-serif);
  }

  /* Language-specific font families */
  body[data-lang="te"] {
    font-family: 'Noto Sans Telugu', 'Inter', sans-serif;
  }

  body[data-lang="hi"] {
    font-family: 'Noto Sans Devanagari', 'Inter', sans-serif;
  }

  body[data-lang="en"] {
    font-family: 'Inter', sans-serif;
  }

  /* Improved focus visibility */
  *:focus-visible {
    @apply outline-none ring-2 ring-primary-500 ring-offset-2;
  }

  /* Better image rendering */
  img {
    @apply max-w-full h-auto;
  }

  /* Remove tap highlight on mobile */
  * {
    -webkit-tap-highlight-color: transparent;
  }
}

/* ============================================================================
   COMPONENT STYLES
   ============================================================================ */

@layer components {
  /* ===== BUTTON STYLES ===== */
  .btn-primary {
    @apply bg-gradient-to-r from-primary-600 to-primary-700 text-white 
           px-4 py-2 rounded-xl font-semibold shadow-sm
           hover:shadow-md hover:from-primary-700 hover:to-primary-800
           focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 
           active:scale-95 transition-all duration-200
           disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none;
  }

  .btn-secondary {
    @apply bg-gradient-to-r from-secondary-600 to-secondary-700 text-white 
           px-4 py-2 rounded-xl font-semibold shadow-sm
           hover:shadow-md hover:from-secondary-700 hover:to-secondary-800
           focus:outline-none focus:ring-2 focus:ring-secondary-500 focus:ring-offset-2 
           active:scale-95 transition-all duration-200
           disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none;
  }

  .btn-outline {
    @apply border-2 border-primary-600 text-primary-600 bg-white
           px-4 py-2 rounded-xl font-semibold
           hover:bg-primary-50 hover:border-primary-700
           focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 
           active:scale-95 transition-all duration-200
           disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none;
  }

  .btn-danger {
    @apply bg-gradient-to-r from-red-600 to-red-700 text-white 
           px-4 py-2 rounded-xl font-semibold shadow-sm
           hover:shadow-md hover:from-red-700 hover:to-red-800
           focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 
           active:scale-95 transition-all duration-200
           disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none;
  }

  .btn-ghost {
    @apply bg-transparent text-gray-700 px-4 py-2 rounded-xl font-semibold
           hover:bg-gray-100 active:bg-gray-200
           focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2 
           active:scale-95 transition-all duration-200
           disabled:opacity-50 disabled:cursor-not-allowed;
  }

  .btn-icon {
    @apply p-2.5 rounded-xl hover:bg-gray-100 active:bg-gray-200
           focus:outline-none focus:ring-2 focus:ring-primary-500 
           active:scale-95 transition-all duration-200;
  }

  /* ===== CARD STYLES ===== */
  .card {
    @apply bg-white rounded-2xl shadow-sm border border-gray-100 p-4
           transition-all duration-300;
  }

  .card-hover {
    @apply card hover:shadow-lg hover:shadow-gray-200/50 hover:border-primary-200 
           hover:-translate-y-1 cursor-pointer;
  }

  .card-glass {
    @apply bg-white/80 backdrop-blur-xl rounded-2xl shadow-lg 
           border border-white/50 p-4;
  }

  .card-gradient {
    @apply bg-gradient-to-br from-white to-gray-50 rounded-2xl shadow-lg 
           border border-gray-100 p-4;
  }

  /* ===== INPUT STYLES ===== */
  .input-field {
    @apply w-full px-4 py-3 border-2 border-gray-200 rounded-xl 
           focus:outline-none focus:ring-4 focus:ring-primary-100 
           focus:border-primary-500 
           hover:border-gray-300
           transition-all duration-200
           placeholder:text-gray-400;
  }

  .input-error {
    @apply border-red-400 focus:ring-red-100 focus:border-red-500
           bg-red-50/30;
  }

  .input-success {
    @apply border-green-400 focus:ring-green-100 focus:border-green-500
           bg-green-50/30;
  }

  /* ===== BADGE STYLES ===== */
  .badge {
    @apply inline-flex items-center gap-1.5 px-3 py-1.5 
           rounded-full text-xs font-semibold
           transition-all duration-200;
  }

  .badge-primary {
    @apply badge bg-gradient-to-br from-primary-100 to-primary-200 
           text-primary-900 shadow-sm shadow-primary-200/50;
  }

  .badge-success {
    @apply badge bg-gradient-to-br from-green-100 to-green-200 
           text-green-900 shadow-sm shadow-green-200/50;
  }

  .badge-warning {
    @apply badge bg-gradient-to-br from-amber-100 to-amber-200 
           text-amber-900 shadow-sm shadow-amber-200/50;
  }

  .badge-danger {
    @apply badge bg-gradient-to-br from-red-100 to-red-200 
           text-red-900 shadow-sm shadow-red-200/50;
  }

  .badge-info {
    @apply badge bg-gradient-to-br from-blue-100 to-blue-200 
           text-blue-900 shadow-sm shadow-blue-200/50;
  }

  .badge-purple {
    @apply badge bg-gradient-to-br from-purple-100 to-purple-200 
           text-purple-900 shadow-sm shadow-purple-200/50;
  }

  .badge-gray {
    @apply badge bg-gradient-to-br from-gray-100 to-gray-200 
           text-gray-800 shadow-sm;
  }

  /* ===== NAVIGATION STYLES ===== */
  .nav-item {
    @apply flex flex-col items-center justify-center gap-1 p-2 
           text-gray-500 rounded-xl
           hover:text-primary-600 hover:bg-primary-50
           active:scale-95
           transition-all duration-200;
  }

  .nav-item-active {
    @apply nav-item text-primary-600 bg-primary-50 font-semibold;
  }

  /* ===== LOADING SKELETON ===== */
  .skeleton {
    @apply bg-gradient-to-r from-gray-200 via-gray-300 to-gray-200 
           rounded-xl animate-shimmer;
    background-size: 200% 100%;
  }

  /* ===== SAFE AREA ===== */
  .safe-area-bottom {
    padding-bottom: env(safe-area-inset-bottom, 1rem);
  }

  .safe-area-top {
    padding-top: env(safe-area-inset-top, 1rem);
  }

  /* ===== VOICE BUTTON PULSE ===== */
  .voice-pulse {
    @apply relative;
  }

  .voice-pulse::before {
    content: '';
    @apply absolute inset-0 rounded-full bg-primary-400 animate-ping opacity-75;
  }

  .voice-pulse::after {
    content: '';
    @apply absolute inset-0 rounded-full bg-primary-500 opacity-20;
  }

  /* ===== STAT CARD STYLES (from Home.jsx) ===== */
  .stat-card {
    @apply relative overflow-hidden rounded-2xl p-5 text-white
           transform transition-all duration-300 
           hover:scale-[1.02] hover:shadow-xl;
  }

  .stat-card-primary {
    @apply stat-card bg-gradient-to-br from-violet-500 to-purple-600
           hover:shadow-violet-500/25;
  }

  .stat-card-success {
    @apply stat-card bg-gradient-to-br from-emerald-500 to-green-600
           hover:shadow-emerald-500/25;
  }

  .stat-card-warning {
    @apply stat-card bg-gradient-to-br from-amber-500 to-orange-600
           hover:shadow-amber-500/25;
  }

  .stat-card-danger {
    @apply stat-card bg-gradient-to-br from-rose-500 to-red-600
           hover:shadow-rose-500/25;
  }

  .stat-card-info {
    @apply stat-card bg-gradient-to-br from-blue-500 to-cyan-600
           hover:shadow-blue-500/25;
  }

  /* ===== PROFILE HEADER STYLES (from Profile.jsx) ===== */
  .profile-header {
    @apply relative overflow-hidden rounded-3xl bg-white shadow-xl;
  }

  .profile-header-gradient {
    @apply absolute inset-0 bg-gradient-to-br from-violet-600 via-purple-600 to-fuchsia-600;
  }

  /* ===== INFO CARD STYLES (from Profile.jsx) ===== */
  .info-card {
    @apply flex items-center gap-4 p-4 rounded-xl 
           hover:shadow-md transition-all duration-300;
  }

  .info-card-violet {
    @apply info-card bg-gradient-to-br from-violet-50 to-purple-50;
  }

  .info-card-blue {
    @apply info-card bg-gradient-to-br from-blue-50 to-cyan-50;
  }

  .info-card-green {
    @apply info-card bg-gradient-to-br from-green-50 to-emerald-50;
  }

  .info-card-amber {
    @apply info-card bg-gradient-to-br from-amber-50 to-orange-50;
  }

  /* ===== QUICK ACTION BUTTON (from Home.jsx) ===== */
  .quick-action-btn {
    @apply flex flex-col items-center gap-3 p-4 rounded-2xl
           bg-gray-50 hover:bg-gray-100
           transform hover:scale-105 hover:-translate-y-1
           transition-all duration-200;
  }

  /* ===== APPOINTMENT CARD (from Home.jsx) ===== */
  .appointment-card {
    @apply relative flex items-center gap-4 p-4 rounded-2xl
           bg-gradient-to-r from-gray-50 to-white
           border border-gray-100 hover:border-gray-200
           hover:shadow-md transition-all duration-200;
  }

  /* ===== SETTINGS ITEM (from Profile.jsx) ===== */
  .settings-item {
    @apply w-full flex items-center justify-between p-4 
           rounded-xl hover:shadow-md transition-all duration-300 cursor-pointer;
  }

  .settings-item-default {
    @apply settings-item bg-gradient-to-br from-gray-50 to-slate-50 hover:bg-gray-100;
  }

  .settings-item-danger {
    @apply settings-item bg-gradient-to-br from-red-50 to-rose-50 hover:bg-red-100;
  }
}

/* ============================================================================
   UTILITY CLASSES
   ============================================================================ */

@layer utilities {
  /* ===== SCROLLBAR STYLES ===== */
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }

  .scrollbar-custom::-webkit-scrollbar {
    width: 8px;
    height: 8px;
  }

  .scrollbar-custom::-webkit-scrollbar-track {
    @apply bg-gray-100 rounded-full;
  }

  .scrollbar-custom::-webkit-scrollbar-thumb {
    @apply bg-gray-300 rounded-full hover:bg-gray-400;
  }

  /* ===== TEXT TRUNCATION ===== */
  .line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .line-clamp-4 {
    display: -webkit-box;
    -webkit-line-clamp: 4;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* ===== TOUCH OPTIMIZATIONS ===== */
  .tap-highlight-none {
    -webkit-tap-highlight-color: transparent;
  }

  .touch-manipulation {
    touch-action: manipulation;
  }

  /* ===== GLASS MORPHISM ===== */
  .glass {
    background: rgba(255, 255, 255, 0.7);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }

  .glass-sm {
    background: rgba(255, 255, 255, 0.5);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }

  .glass-lg {
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }

  .glass-dark {
    background: rgba(0, 0, 0, 0.3);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }

  .glass-primary {
    background: rgba(139, 92, 246, 0.1);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }

  .glass-white {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }

  /* ===== TEXT GRADIENTS ===== */
  .text-gradient-primary {
    @apply bg-gradient-to-r from-violet-600 to-purple-600 bg-clip-text text-transparent;
  }

  .text-gradient-success {
    @apply bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent;
  }

  /* ===== SHADOW UTILITIES ===== */
  .shadow-glow-primary {
    box-shadow: 0 0 20px rgba(139, 92, 246, 0.3);
  }

  .shadow-glow-success {
    box-shadow: 0 0 20px rgba(16, 185, 129, 0.3);
  }

  .shadow-glow-danger {
    box-shadow: 0 0 20px rgba(239, 68, 68, 0.3);
  }

  /* ===== BORDER LEFT ACCENT ===== */
  .border-l-accent-violet {
    @apply border-l-4 border-l-violet-500;
  }

  .border-l-accent-purple {
    @apply border-l-4 border-l-purple-500;
  }

  .border-l-accent-blue {
    @apply border-l-4 border-l-blue-500;
  }

  .border-l-accent-green {
    @apply border-l-4 border-l-green-500;
  }
}

/* ============================================================================
   RESPONSIVE BREAKPOINTS (Extra Small)
   ============================================================================ */

@media (min-width: 375px) {
  .xs\:inline {
    display: inline;
  }
  .xs\:hidden {
    display: none;
  }
  .xs\:flex {
    display: flex;
  }
  .xs\:block {
    display: block;
  }
}

/* ============================================================================
   ANIMATIONS - FROM ALL COMPONENTS
   ============================================================================ */

/* ===== GRADIENT ANIMATION (Home.jsx) ===== */
@keyframes gradient-x {
  0%, 100% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
}

.animate-gradient-x {
  background-size: 200% 200%;
  animation: gradient-x 15s ease infinite;
}

/* ===== WAVE ANIMATION (Home.jsx - Emoji) ===== */
@keyframes wave {
  0%, 100% { 
    transform: rotate(0deg); 
  }
  25% { 
    transform: rotate(20deg); 
  }
  75% { 
    transform: rotate(-15deg); 
  }
}

.animate-wave {
  display: inline-block;
  animation: wave 1.5s ease-in-out infinite;
  transform-origin: 70% 70%;
}

/* ===== FADE ANIMATIONS ===== */
@keyframes fade-in {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-in {
  animation: fade-in 0.4s ease-out forwards;
}

@keyframes fade-in-up {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-in-up {
  animation: fade-in-up 0.5s ease-out forwards;
}

@keyframes fade-in-down {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-in-down {
  animation: fade-in-down 0.5s ease-out forwards;
}

/* ===== BLOB ANIMATION (DoctorLayout.jsx) ===== */
@keyframes blob {
  0%, 100% {
    transform: translate(0, 0) scale(1);
  }
  25% {
    transform: translate(20px, -30px) scale(1.1);
  }
  50% {
    transform: translate(-20px, 20px) scale(0.95);
  }
  75% {
    transform: translate(30px, 10px) scale(1.05);
  }
}

.animate-blob {
  animation: blob 20s ease-in-out infinite;
}

/* ===== FLOAT ANIMATION (DoctorLayout.jsx) ===== */
@keyframes float {
  0%, 100% { 
    transform: translate(0, 0) scale(1); 
  }
  25% { 
    transform: translate(10px, -20px) scale(1.05); 
  }
  50% { 
    transform: translate(-15px, 10px) scale(0.95); 
  }
  75% { 
    transform: translate(20px, 15px) scale(1.02); 
  }
}

.animate-float {
  animation: float 20s ease-in-out infinite;
}

/* ===== ANIMATION DELAYS ===== */
.animation-delay-500 {
  animation-delay: 0.5s;
}

.animation-delay-1000 {
  animation-delay: 1s;
}

.animation-delay-2000 {
  animation-delay: 2s;
}

.animation-delay-3000 {
  animation-delay: 3s;
}

.animation-delay-4000 {
  animation-delay: 4s;
}

/* ===== SCALE ANIMATION (PhoneInput.jsx, Badge.jsx) ===== */
@keyframes scale-in {
  from {
    transform: scale(0);
    opacity: 0;
  }
  50% {
    transform: scale(1.2);
  }
  to {
    transform: scale(1);
    opacity: 1;
  }
}

.animate-scale-in {
  animation: scale-in 0.3s ease-out forwards;
}

@keyframes scale-up {
  from {
    transform: scale(0.95);
    opacity: 0;
  }
  to {
    transform: scale(1);
    opacity: 1;
  }
}

.animate-scale-up {
  animation: scale-up 0.2s ease-out forwards;
}

/* ===== SHAKE ANIMATION (PhoneInput.jsx) ===== */
@keyframes shake {
  0%, 100% {
    transform: translateX(0);
  }
  25% {
    transform: translateX(-5px);
  }
  75% {
    transform: translateX(5px);
  }
}

.animate-shake {
  animation: shake 0.4s ease-in-out;
}

/* ===== DROPDOWN ANIMATIONS (Header.jsx) ===== */
@keyframes dropdown-fade {
  from { 
    opacity: 0; 
    transform: translateY(-8px) scale(0.96); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0) scale(1); 
  }
}

.animate-dropdown-fade {
  animation: dropdown-fade 0.2s ease-out forwards;
}

@keyframes dropdown-slide {
  from { 
    opacity: 0; 
    transform: translateY(-12px) scale(0.95);
  }
  to { 
    opacity: 1; 
    transform: translateY(0) scale(1);
  }
}

.animate-dropdown-slide {
  animation: dropdown-slide 0.25s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

/* ===== SLIDE ANIMATIONS ===== */
@keyframes slide-in-right {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

.animate-slide-in-right {
  animation: slide-in-right 0.3s ease-out forwards;
}

@keyframes slide-in-left {
  from {
    transform: translateX(-100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

.animate-slide-in-left {
  animation: slide-in-left 0.3s ease-out forwards;
}

@keyframes slide-in-up {
  from {
    transform: translateY(100%);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.animate-slide-in-up {
  animation: slide-in-up 0.3s ease-out forwards;
}

@keyframes slide-in-down {
  from {
    transform: translateY(-100%);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.animate-slide-in-down {
  animation: slide-in-down 0.3s ease-out forwards;
}

/* ===== PULSE ANIMATIONS ===== */
@keyframes pulse-glow {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(139, 92, 246, 0.4);
  }
  50% {
    box-shadow: 0 0 0 12px rgba(139, 92, 246, 0);
  }
}

.animate-pulse-glow {
  animation: pulse-glow 2s ease-in-out infinite;
}

@keyframes pulse-glow-success {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.4);
  }
  50% {
    box-shadow: 0 0 0 12px rgba(16, 185, 129, 0);
  }
}

.animate-pulse-glow-success {
  animation: pulse-glow-success 2s ease-in-out infinite;
}

@keyframes pulse-glow-danger {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.4);
  }
  50% {
    box-shadow: 0 0 0 12px rgba(239, 68, 68, 0);
  }
}

.animate-pulse-glow-danger {
  animation: pulse-glow-danger 2s ease-in-out infinite;
}

/* ===== BOUNCE ANIMATIONS ===== */
@keyframes bounce-subtle {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-5px);
  }
}

.animate-bounce-subtle {
  animation: bounce-subtle 1s ease-in-out infinite;
}

@keyframes bounce-in {
  0% {
    transform: scale(0);
    opacity: 0;
  }
  50% {
    transform: scale(1.1);
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
}

.animate-bounce-in {
  animation: bounce-in 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55) forwards;
}

/* ===== SPIN ANIMATIONS ===== */
@keyframes spin-slow {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

.animate-spin-slow {
  animation: spin-slow 3s linear infinite;
}

/* ===== SHIMMER ANIMATION (Skeleton Loading) ===== */
@keyframes shimmer {
  0% {
    background-position: -200% 0;
  }
  100% {
    background-position: 200% 0;
  }
}

.animate-shimmer {
  animation: shimmer 1.5s ease-in-out infinite;
}

/* ===== EXPAND ANIMATION ===== */
@keyframes expand {
  from {
    transform: scaleY(0);
    opacity: 0;
  }
  to {
    transform: scaleY(1);
    opacity: 1;
  }
}

.animate-expand {
  transform-origin: top;
  animation: expand 0.2s ease-out forwards;
}

/* ===== COLLAPSE ANIMATION ===== */
@keyframes collapse {
  from {
    transform: scaleY(1);
    opacity: 1;
  }
  to {
    transform: scaleY(0);
    opacity: 0;
  }
}

.animate-collapse {
  transform-origin: top;
  animation: collapse 0.2s ease-in forwards;
}

/* ============================================================================
   TRANSITIONS
   ============================================================================ */

.transition-smooth {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.transition-bounce {
  transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.transition-spring {
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

.transition-fast {
  transition: all 0.15s ease-out;
}

/* ============================================================================
   PATTERN BACKGROUNDS
   ============================================================================ */

.bg-pattern-grid {
  background-image: 
    linear-gradient(rgba(0,0,0,0.03) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0,0,0,0.03) 1px, transparent 1px);
  background-size: 20px 20px;
}

.bg-pattern-dots {
  background-image: radial-gradient(circle at 2px 2px, rgba(0,0,0,0.1) 1px, transparent 0);
  background-size: 24px 24px;
}

.bg-pattern-lines {
  background-image: repeating-linear-gradient(
    -45deg,
    transparent,
    transparent 10px,
    rgba(0,0,0,0.02) 10px,
    rgba(0,0,0,0.02) 20px
  );
}

/* ============================================================================
   PRINT STYLES
   ============================================================================ */

@media print {
  body {
    @apply bg-white;
  }

  .no-print {
    display: none !important;
  }

  .card {
    @apply shadow-none border;
  }
}

/* ============================================================================
   REDUCED MOTION (Accessibility)
   ============================================================================ */

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }

  .animate-blob,
  .animate-float,
  .animate-wave,
  .animate-pulse-glow,
  .animate-bounce-subtle {
    animation: none !important;
  }
}

/* ============================================================================
   DARK MODE SUPPORT (Optional - Future Use)
   ============================================================================ */

@media (prefers-color-scheme: dark) {
  /* Uncomment when implementing dark mode
  :root {
    --color-bg: #1a1a2e;
    --color-text: #eaeaea;
  }
  */
}